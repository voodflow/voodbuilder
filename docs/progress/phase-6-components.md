# Phase 6 — Components module

## Boundary

- `ComponentsModule` owns components + global-classes Editor routes
- Editor `componentsUrl` / `globalClassesUrl` gated (page + chrome editors)
- Public component CSS scoping/render remains Core

## Config

`voodbuilder.modules.components.enabled` / `VOODBUILDER_MODULE_COMPONENTS`

## Tests

- `tests/Modules/ComponentsModuleTest.php`

## Remaining Phase 6 order

Popups
