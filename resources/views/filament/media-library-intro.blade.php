{{--
  Intro copy for the Filament media library page.
  The Editor frontend picker (GrapesJS AssetManager) reads the same Spatie collections via JSON.
--}}
<div class="fi-prose text-sm text-gray-600 dark:text-gray-300">
    <p>{{ __('voodbuilder::admin.media_library.intro') }}</p>
</div>
